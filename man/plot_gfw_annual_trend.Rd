% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biology.R
\name{plot_gfw_annual_trend}
\alias{plot_gfw_annual_trend}
\title{Plot annual total fishing effort trend for the Antarctic Peninsula}
\usage{
plot_gfw_annual_trend(
  gfw_df,
  trend_start,
  trend_end,
  lat_min = -80,
  lat_max = -35,
  lon_min = -75,
  lon_max = -55
)
}
\arguments{
\item{gfw_df}{Data frame. Raw output from \code{\link[=download_gfw_data]{download_gfw_data()}}.}

\item{trend_start}{Integer. First year shown on the x-axis.}

\item{trend_end}{Integer. Last year shown on the x-axis.}

\item{lat_min}{Numeric. Southern latitude boundary for filtering.
Defaults to \code{-80}.}

\item{lat_max}{Numeric. Northern latitude boundary for filtering.
Defaults to \code{-35}.}

\item{lon_min}{Numeric. Western longitude boundary for filtering.
Defaults to \code{-75}.}

\item{lon_max}{Numeric. Eastern longitude boundary for filtering.
Defaults to \code{-55}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Displays total apparent fishing hours per year as a scatter-line chart
overlaid with a LOESS smooth (95\% CI). Each point is labelled with its
rounded value and colour-coded by magnitude.
}
\examples{
\dontrun{
gfw_df <- download_gfw_data(years = c(2016:2025))
p <- plot_gfw_annual_trend(gfw_df, trend_start = 2016, trend_end = 2020)
print(p)
}

}

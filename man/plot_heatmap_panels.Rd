% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biology.R
\name{plot_heatmap_panels}
\alias{plot_heatmap_panels}
\title{Plot annual fishing-effort heatmap panels for the Antarctic Peninsula}
\usage{
plot_heatmap_panels(
  heatmap_df,
  years,
  ncol = 2,
  xlim = c(-80, -35),
  ylim = c(-75, -55)
)
}
\arguments{
\item{heatmap_df}{Data frame. Raw output from \code{\link[=fetch_heatmap_data]{fetch_heatmap_data()}}.}

\item{years}{Integer vector. Years to display (one panel each).}

\item{ncol}{Integer. Number of columns in the panel layout.
Defaults to \code{2}.}

\item{xlim}{Numeric vector of length 2. Longitude limits of the
map window. Defaults to \code{c(-80, -35)}.}

\item{ylim}{Numeric vector of length 2. Latitude limits of the
map window. Defaults to \code{c(-75, -55)}.}
}
\value{
A \link[patchwork:patchwork-package]{patchwork::patchwork-package} object containing all annual panels plus a shared
title and caption. Print or pass to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
Produces a multi-panel map (one panel per year) showing AIS-based apparent
fishing hours per 0.1° grid cell on a log₁₀ scale. All panels share the
same colour axis for direct year-to-year comparison.
}
\examples{
\dontrun{
heatmap_df <- fetch_heatmap_data(2016:2025)
p <- plot_heatmap_panels(heatmap_df, years = 2016:2025)
print(p)
ggplot2::ggsave("GFW_plot.pdf", p, width = 12, height = 16)
}

}

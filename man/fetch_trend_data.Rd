% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biology.R
\name{fetch_trend_data}
\alias{fetch_trend_data}
\title{Fetch and cache monthly fishing-effort trend data}
\usage{
fetch_trend_data(
  trend_start,
  current_year,
  ccamlr_id = "CCAMLR",
  key = gfwr::gfw_auth(),
  cache_file = "cache_trend_data.rds"
)
}
\arguments{
\item{trend_start}{Integer. First year of the trend period (inclusive).}

\item{current_year}{Integer. Last year of the trend period (inclusive).}

\item{cache_file}{Character. Path to the RDS cache file. Defaults to
\code{"cache_trend_data.rds"}.}
}
\value{
A data frame with one row per year × flag × month × grid cell,
containing raw GFW API columns plus an integer \code{year} column.
}
\description{
Queries the GFW API for monthly, flag-grouped fishing effort over the
CCAMLR convention area for each year in \code{trend_start:current_year}.
Results are saved to an RDS cache file. The cache is validated on load and
automatically replaced if empty or corrupt.
}
\examples{
\dontrun{
trend_df  <- fetch_trend_data(2016, 2025, ccamlr_id, key)
}

}
\seealso{
\code{\link[=download_gfw_year]{download_gfw_year()}}, \code{\link[=fetch_heatmap_data]{fetch_heatmap_data()}}
}
